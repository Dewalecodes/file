
const aboutTime = function (month, season){
    console.log(`the month is ${month} and it is a ${season} season`)
}
const dMath = (nnumber, name)=>{
    console.log(`number ${nnumber} is for the name ${name}`)
}
module.exports = dMath